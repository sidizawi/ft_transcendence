{
  "name": "db",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --watch . --exec node index.js"
  },
  "dependencies": {
    "@fastify/jwt": "^9.0.0",
    "@fastify/cors": "^10.0.2",
    "better-sqlite3": "^11.9.1",
    "dotenv": "^16.5.0",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
