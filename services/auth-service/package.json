{
	"name": "auth-service",
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "nodemon --watch . --exec node index.js"
  	},
	"dependencies": {
		"fastify": "^5.0.0",
		"bcrypt": "^5.1.1",
		"@fastify/cors": "^10.0.2",
		"better-sqlite3": "^11.8.1",
		"fastify-cors": "^6.0.3"
	},
	"devDependencies": {
		"nodemon": "^2.0.20"
  	}
  }
  