{
  "name": "front",
  "version": "1.0.0",
  "main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "nodemon --watch . --exec node index.js"
  },
  "dependencies": {
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
