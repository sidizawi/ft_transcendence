ajouter status quand co et deco
ajouter status partout
Ajout notif si new message + new demande de connexion


	friendsTab:
		- pourquoi se referme entre profile et reste?
		- Bon comportement sur friendProfile quand chqngement de friendStatus faire pareil partout
			-> ne se ferme pas, rafraichit

	error and warning:
		- check 401, token expired
		- check all error code from back are the same in the front
		- check all error type from back are the same in the front

	!!! change status of the user: connected or not, via chatService

	Pong:
		- block up and down scrolling
		- close websocket when changing history

	Tournament:
		- send message to user (remote)
		- remote
		- pong

	connect4:
		- matchmaking
	
	user:
		- add level

	Settings page:
		- changer window.href.location ----> refresh pour rediriger
		- Tous les messages d'erreurs quand delete formulaire

	auth.ts:
		- Google logo

	Friends page:
		- Search barre -> API ou fetch dymamique -> web socket? :(((
		- I add someone, that person add me => error


	Sidi:
		- remove all '!' chars
		- check Map variables

	BACK:
		- pas safe acceder image quand pas log + ajout ID personne devant nom img?
		- twoFactorAuth/translations:
			- langue des mails
				- errror code unifie qu'on peut traduire dans dico
		- Attention message erreur quand vient du back -> ne peut traduire

	PARTOUT:
		- chgmnt TOKEN force, attention, class user ne se met pas a jour car pas d'appel de back
		- probleme TRANSLATION (i.e. tous les champs vide dans signup)
		- si token non re-sent (supp ou autre) rediriger vers sign in
		- / accepter comme URL?
	



	Visuel:
		-Footer:
			- group name or names
		-Random Page:
			- SCROLL ?????
		- Other:
			- enlever le hover 2FA quand code pas entier?


	Functionnality to check at the END:
		- Traductions
		- Browsers Compatibility
		- Accessibility (?)
		- SSR (?)

	Files:
		- Clean up unused datas

	websocket:
		- websocket disconnected: timeout
		- new tab






	connect 4:
		- add translation
		- create AI algo
		- add timer
		- vusu coin derriere le front
		- check if they're friend
		- check that the user can open only one game (open play with friend -> then play with AI (bug))

	tournament:
		- create tournament page: name, public | private, code, capacity
		- join a tournament: list tournament (to join a private one, it needs a code)
		- waiting page
		- game page: where we can see each group
		- canvas
		- add previous and next btn
		- save to db

	games:
		- check how to change href

	chat:
		- check connection
		- update read
		- add a general chat

	pong:
		- check if the websocket is closed




      let url = "http://localhost:3001/login";
      url = url.replace("localhost", window.location.hostname);
      console.log(url);

      const response = await fetch('http://' + window.location.hostname + ':3001/login', {


//Google just en localhost -> a distance pb securite Google


L - D
0 - 4
1 - 3
2 - 2
3 - 1
4 - 0
