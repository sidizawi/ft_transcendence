To Do:

ajouter status quand co et deco
ajouter status friends.ts
si token non resent (supp ou autre) rediriger vers sign in

	friendsTab:
		- refresh quand chgmnt AMIS (friends.ts)

	auth.ts:
		- Google logo

	twoFactorAuth/translations
		- title -> 2FAVerification (pop up dans profile ex)
		- langue des mails

	Settings page:
		- changer window.href.location ----> refresh pour rediriger
		- ok si meme mdp????

	Friends page:
		- Search barre -> API ou fetch dymamique -> web socket? :(((

	PARTOUT:
		- console du navigateur renvoit ERREUR
		- chgmnt TOKEN force, attention, class user ne se met pas a jour car pas d'appel de back
		- probleme TRANSLATION (i.e. tous les champs vide dans signup)
		-	Attention message erreur quand vient du back -> ne peut traduire

	
	FriendsProfile:
		- Erreur avatar quand NULL
	BACK:
		- pas safe acceder image quand pas log + ajout ID personne devant nom img?
		- ajouter dans db /img/default.jpg si rien
	
	Visuel:
		-Profile page:
			- Game Tab
		-Footer:
			- Names (?)
		- Header:
			- Fixed or moving (?)
		-Random Page:
			- SCROLL ?????
		- Other:
			- enlever le hover 2FA quand code pas entier?
			- Colour for black texts in light mode (?)
			- Back buttons
			- SVG friendsProfile.ts
			- ADD/DELETE colours in Dark Mode
			- Change colour Dark Mode (?)



	Functionnality to check at the END:
		- Traductions
		- Browsers Compatibility
		- Accessibility (?)
		- SSR (?)
		- when reload on page it should load that page: /profile and reload => go to profile, not home

	Files:
		- Clean up unused datas

	connect 4:
		- create AI algo
		- add timer
		- vusu coin derriere le front

	pong:
		- check if the websocket is closed




      let url = "http://localhost:3001/login";
      url = url.replace("localhost", window.location.hostname);
      console.log(url);

      const response = await fetch('http://' + window.location.hostname + ':3001/login', {


//Google just en localhost -> a distance pb securite Google